# 百词斩助手浏览器插件样式总结

本文基于当前 `baicizhan-helper` 源码整理，覆盖浏览器工具栏中的 popup 页面，以及注入网页的 content-script 取词浮层、提示和 Anki 导出弹窗。本文描述的是**当前实现**，不是一套已经完全统一的设计规范。

## 1. 整体风格定位

插件整体采用简洁、实用、弱装饰的工具型界面：白色或浅灰背景、蓝色功能强调、适度圆角和轻阴影，重点突出搜索、查词、收藏和导出等操作，不使用渐变、复杂插画或大面积品牌装饰。

视觉上可以概括为：

- **产品气质**：轻量、克制、偏效率工具。
- **基础风格**：接近 Bootstrap 的灰白后台组件，同时混合少量 Tailwind 风格的中性灰色阶。
- **品牌识别**：蓝底白色书本 Logo；功能色主要使用蓝色，但具体蓝色色值尚未完全统一。
- **信息组织**：小尺寸容器内以紧凑排列为主，使用边框、分隔线、留白和字号层级组织内容。
- **动效**：以 `0.2s`～`0.3s` 的颜色、透明度、位移和缩放过渡为主，反馈直接，不强调装饰性动画。

## 2. 基础视觉语言

### 2.1 字体

popup 的主字体为系统无衬线字体栈：

```css
-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif
```

content-script 中存在两套字体：

- 外层浮动控件使用 `ui-sans-serif, system-ui, sans-serif`。
- 单词详情卡片使用 `Arial, sans-serif`。
- Anki 导出弹窗使用完整系统字体栈。

整体是清晰、中性的系统字体风格，但 popup、单词卡片和导出弹窗之间尚未统一字体声明。

### 2.2 颜色

当前主要色彩角色如下：

| 角色 | 代表色值 | 主要用途 |
| --- | --- | --- |
| 品牌/主操作蓝 | `#007bff`、`#288cd7`、`#1976d2` | 开关、收藏状态、登录链接、content-script 浮动按钮 |
| 模态框强调紫蓝 | `#6366f1` | Anki 弹窗表单聚焦和预期的导出主按钮 |
| 成功绿 | `#28a745`、`#4CAF50`、`#22c55e` | 微信绑定、验证码、进度条 |
| 危险红 | `#dc3545`、`#dc2626`、`#F44336` | 退出、错误、会员升级提示 |
| 警告橙 | `#f59f00` | 登录过期状态 |
| 主文字 | `#1f2937`、`#212529`、`#2c3e50` | 标题、单词、正文主信息 |
| 次级文字 | `#495057`、`#6c757d`、`#7f8c8d` | 音标、翻译、标签、辅助信息 |
| 浅色背景 | `#ffffff`、`#f8f9fa`、`#f5f5f5` | 卡片、导航、页面背景 |
| 浅色边框 | `#e9ecef`、`#d1d5db`、`#c0c0c0` | 分隔线、输入框、卡片边界 |
| 深色背景 | `#1a1a1a`、`#212529`、`#2d2d2d` | popup 和单词卡片的暗色主题 |
| 深色文字 | `#e0e0e0`、`#b0b0b0` | 暗色主题主、次文字 |

整体配色偏中性，蓝色负责功能强调，红、绿、橙只承担明确状态含义。当前主蓝同时存在 Bootstrap 蓝、品牌蓝和 Indigo，视觉上略有分散。

### 2.3 圆角、边框与阴影

- 常规输入框、卡片和操作控件主要使用 `6px`～`8px` 圆角。
- 登录卡片和 Anki 弹窗使用 `12px` 圆角，层级更高、视觉更柔和。
- 头像和浮动按钮使用圆形。
- 边框通常为 `1px` 浅灰；搜索框使用 `2px` 边框以强化输入区域。
- 普通卡片使用轻阴影，如 `0 4px 8px rgba(0,0,0,.1)`。
- 登录卡片和模态框使用更大的 `20px`～`24px` 模糊阴影，表示更高层级。

### 2.4 间距与尺寸

- popup 固定宽度为 `400px`，主容器最大高度为 `540px`。
- popup 顶部导航高度仅 `35px`，属于紧凑型工具栏。
- 常用内容内边距为 `10px`、`12px`、`16px`、`20px`。
- 常用元素间距为 `4px`、`8px`、`12px`、`16px`、`24px`。
- content-script 单词卡片固定宽度为 `360px`；Anki 弹窗固定宽度为 `320px`。
- 正文多为 `14px`～`16px`，单词标题为 `24px`，popup 搜索结果单词为 `18px`。

## 3. Popup 页面风格

### 3.1 整体框架

popup 使用固定宽度的纵向布局：顶部导航 + 可滚动主内容区。

- 外层 `.app-container` 为白色卡片，圆角 `8px`，带轻阴影。
- 顶部 `.top-navbar` 使用浅灰背景和底部分隔线。
- 导航只展示文字“主页 / 我的 / 设置 / 反馈”，不依赖复杂图标。
- Logo 为 `24px × 24px`，旁边直接展示版本号。
- 主内容区使用 `10px` 内边距，内容过长时纵向滚动。

这套布局强调功能密度，视觉接近小型桌面工具面板。

### 3.2 首页与搜索

首页的核心视觉焦点是搜索框：

- 浅灰底、`2px` 浅灰边框、`8px` 圆角。
- 输入区和搜索按钮在同一个容器中，无额外卡片包裹。
- 搜索按钮默认是低饱和灰色，悬停时略微加深，视觉优先级较低。
- 搜索结果采用无卡片列表，每项仅通过底部分隔线区分。
- 单词使用 `18px / 600`，释义使用较小的灰色文字。
- hover 只改变浅灰背景，交互反馈克制。
- 滚动条宽度为 `6px`，颜色与页面中性灰体系一致。

选择结果后，首页直接复用 content-script 的单词详情组件，因此 popup 与网页取词浮层在核心单词内容上保持一致。

### 3.3 登录页

登录页是居中的独立表单卡片：

- 白色背景、`12px` 圆角和较明显阴影。
- 标题较大，CSS 定义为 `32px`，强调页面任务。
- 输入框和按钮高度约 `50px`，比其他 popup 页面更宽松。
- 验证码按钮使用成功绿，登录按钮使用蓝色；禁用状态统一变灰并降低透明度。
- 错误提示使用浅红背景、红色边框和红色文字。

当前登录页大量样式写在 JSX 内联样式中，实际表现与 `Login.css` 中的部分定义并不完全一致；视觉结论应以内联样式优先。

### 3.4 设置页

设置页采用典型的两列表单布局：左侧标签，右侧控件。

- 每项使用 `grid-template-columns: 1fr auto`。
- 项目之间通过底部分隔线区分，不额外使用卡片。
- 标签为 `14px / 500`，控件保持紧凑。
- 下拉框使用 `6px` 圆角和浅灰边框。
- 普通开关为 `44px × 24px`，蓝色表示开启。
- 英音/美音选择采用分段式胶囊开关，蓝色滑块表示当前选项。
- 页面底部说明文字和网页端链接以普通正文呈现。

### 3.5 用户信息页

用户页是居中、纵向排列的资料卡风格：

- 使用圆形头像占位，浅灰背景与边框，内部为用户图标。
- 用户名、手机号和微信绑定状态集中在头像下方。
- 账号类型置于独立浅灰信息块中，使用边框和 `8px` 圆角。
- 账号类型值使用蓝色；登录过期使用橙色；会员升级使用带下划线的红色链接。
- 退出按钮采用危险红，重新登录按钮采用主蓝。
- 按钮按下时下移 `1px`，提供轻量物理反馈。

### 3.6 Popup 暗色主题

popup 支持手动切换浅色/深色主题，深色状态通过 `body.dark-theme` 和 `.app-container.dark-theme` 控制。

- 主背景：`#1a1a1a`。
- 导航和输入表面：`#2d2d2d`。
- 边框：`#3d3d3d` 或 `#495057`。
- 主文字：`#e0e0e0`。
- 次级文字：`#b0b0b0` 或 `#888888`。

暗色主题已覆盖导航、首页搜索、搜索结果、滚动条、设置控件和用户信息主体，但登录页、部分说明文字和少数控件没有完整的暗色样式。

## 4. Content-script 弹窗组件风格

content-script 注入所有网页，核心设计目标是：体积小、层级高、样式尽量不影响宿主页面。

### 4.1 划词触发图标

- 用户选中英文单词后，在鼠标附近显示 `32px × 32px` 的白色圆形按钮。
- 按钮使用轻阴影，内部放置约 `19px` 的品牌 Logo。
- 出现时执行 `0.2s` 的淡入和缩放动画：从 `0.8` 放大到 `1`。
- 使用 `z-index: 10000`，确保浮在网页内容上方。

这是 content-script 中品牌感最明确的入口，同时保持了较低的视觉侵入性。

### 4.2 单词详情浮层

取词结果使用固定宽度 `360px` 的白色词典卡片：

- `10px` 内边距、`8px` 圆角、浅灰边框和轻阴影。
- 内容纵向排列，模块间距为 `8px`。
- 单词标题使用 `24px / 600`，是最强视觉层级。
- 收藏星标位于标题行右侧；已收藏为蓝色，未收藏为浅灰。
- 英/美音标并排显示，文字和音频图标都使用中性灰。
- 中文释义为 `15px`，词性加粗，过长内容截断到约 50 个字符。
- 分隔线后展示例句、翻译和图片。
- 英文例句使用斜体，翻译使用更小、更浅的文字。
- 例句图片最大宽度 `300px`，圆角 `4px`。

浮层通过 Radix Popover 锚定选中文本，箭头为白色，打开时使用与划词图标一致的淡入缩放动画。

### 4.3 错误浮层

错误状态使用 `200px × 200px` 的固定方形卡片：

- 内容水平、垂直居中。
- 白底、浅灰边框、`8px` 圆角。
- 文字加粗，登录链接使用蓝色下划线。

它的视觉结构简单明确，但尺寸固定，和正常单词卡片的自适应内容方式不同。

### 4.4 Anki 导出弹窗

Anki 导出使用全屏遮罩模态框：

- 遮罩为 `rgba(0,0,0,.5)`，内容水平垂直居中。
- 弹窗宽度 `320px`，白底、`12px` 圆角、大阴影。
- 头部、正文、底部操作区通过分隔线和浅灰背景区分。
- 标题为 `16px / 600`，关闭按钮为灰色大号 `×`。
- 表单控件为浅灰底、`6px` 圆角；聚焦时使用 Indigo 边框和外发光。
- 取消按钮为白底描边；导出按钮设计意图为 Indigo 主按钮。
- 导出进度条使用绿色，并叠加循环移动的斜纹动画。
- 错误提示使用浅红底、红色文字；说明文字使用较小的中性灰。

该弹窗的视觉最接近现代 SaaS 组件，与 popup 的 Bootstrap 蓝灰风格存在一定差异。

### 4.5 Toast 提示

收藏成功、设置错误等短反馈使用顶部居中的黑色半透明提示：

- 位于视口顶部约 `20%`。
- 黑色 `80%` 透明背景、白字、`4px` 圆角。
- 字号 `14px`，内容宽度限制为约 `10ch`～`20ch`。
- 显示 3 秒后自动消失。

这是典型的弱打扰 Toast，不提供按钮或复杂状态。

### 4.6 Content-script 暗色主题

暗色主题目前主要覆盖单词详情卡片：

- 卡片背景切换为 `#1a1a1a`。
- 单词、音标和例句切换为 `#e0e0e0`。
- 释义切换为 `#b0b0b0`。

错误浮层、Toast 和 Anki 导出弹窗没有跟随主题切换，因此 content-script 的暗色支持属于局部支持。

## 5. 交互反馈特征

- 可点击元素普遍使用 `cursor: pointer`。
- hover 主要通过背景色或文字颜色加深表达。
- 输入框 focus 主要通过蓝、绿或 Indigo 边框表达。
- 禁用按钮使用灰色、较低透明度和 `not-allowed` 光标。
- 高风险操作使用红色，成功或连接状态使用绿色。
- 弹出元素采用短促淡入/缩放；进度状态使用持续斜纹动画。
- 没有复杂页面转场，也没有大幅位移动画。

## 6. 当前样式一致性与实现边界

### 6.1 已形成的一致特征

- 系统无衬线字体、浅色表面、中性灰文字。
- `6px`～`8px` 常规圆角，轻边框、轻阴影。
- 蓝色表示主要操作或已选状态。
- 红、绿、橙分别表达危险、成功、警告。
- popup 和浮层都优先保证紧凑、清晰和操作效率。
- content-script 的主要类名使用 `bcz-helper-` 前缀，降低与宿主网页冲突的概率。

### 6.2 尚未统一或需要注意的地方

- 主色同时使用 `#007bff`、`#288cd7`、`#1976d2`、`#6366f1` 和 `#4CAF50`，缺少统一 token。
- 字体在系统字体、Arial 和 `ui-sans-serif` 之间切换。
- 登录页同时存在 CSS 和大量内联样式，维护时容易出现覆盖关系不清。
- popup 的 `index.css` 仍保留 Vite 模板样式，例如 `#app`、通用 `h1` 和 `.card`，与实际入口 `#root` 不完全对应。
- content-script 虽然大部分类名已加前缀，但 `.dark-theme` 等选择器仍可能受到宿主页面样式影响；当前没有使用 Shadow DOM 做完全隔离。
- 暗色主题覆盖不完整，登录页、错误卡片、Toast 和 Anki 弹窗没有统一适配。
- `AnkiExport.css` 中主按钮基础样式写在 `.export-button`，实际组件使用 `.bcz-helper-export-button`；当前导出按钮可能缺少预期的默认 Indigo 背景。
- `App.css` 中部分声明把 CSS 值写成了带引号的字符串，如 `padding: '0'`，浏览器会忽略这些无效声明。
- popup 固定 `400px`，content-script 卡片和弹窗也使用固定宽度，整体更适合桌面浏览器，不是响应式优先设计。

## 7. 风格关键词

`轻量工具`、`灰白界面`、`蓝色强调`、`紧凑布局`、`系统字体`、`轻圆角`、`弱阴影`、`状态色明确`、`低干扰浮层`、`局部暗色模式`

## 8. 主要样式来源

### Popup

- `src/popup/index.css`
- `src/popup/pages/Home.css`
- `src/popup/pages/Login.css`
- `src/popup/pages/Settings.css`
- `src/popup/pages/UserInfo.css`
- `src/popup/pages/Layout.tsx`
- `src/popup/pages/Login.tsx`（包含较多内联样式）

### Content-script

- `src/content/views/App.css`
- `src/components/PopoverContent.css`
- `src/components/AnkiExport.css`
- `src/components/Tips.css`
- `src/content/views/App.tsx`（包含定位相关内联样式）
- `src/components/PopoverContent.tsx`（包含图标状态色等内联样式）

